name: Test workflow triggers

on:
  push:
    tags:
      - v*
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test release tag trigger
      run: |
        echo "Hello, World"